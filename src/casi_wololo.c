#include "../inc/philo.h"

/*
int	ft_lst_len(t_philo *lst_philo)
{
	t_philo	*aux;
	int		i;

	if (!lst_philo)
		return (0);
	i = 0;
	aux = lst_philo;
	while (aux)
	{
		aux = aux->right;
		i++;
	}
	printf("Longitud lista: %d\n", i);
	return (i);
}
*/